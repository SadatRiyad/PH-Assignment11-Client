import { useLoaderData } from "react-router-dom";

const QueryDetails = () => {
    const data = useLoaderData();
    console.log(data)
    return (
        <div>
            
        </div>
    );
};

export default QueryDetails;